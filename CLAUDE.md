# パチンコホール分析アプリ プロジェクト

## プロジェクト概要

パチンコホール向けデータ分析Webアプリを構築・継続開発している。プロジェクトオーナーはブラスト。コーディングはすべてClaude（このCLAUDE.mdを読んでいるあなた）が担当し、ブラストはデプロイ・ブラウザ実機確認・パチンコ業界のドメイン知識の提供を担う。

**目的**：翌日に高設定を受けそうな台を特定し、戦略的な立ち回りを支援する。

## ユーザー（ブラスト）について・行動指針（必読）

- **技術知識は初心者レベル**。専門用語・操作手順はその都度丁寧に説明する。
- **コードは全てClaudeが書く。ユーザーは実行するだけ。** 手打ちコマンドを要求しない。
- **エビデンスのある事実だけを述べる**。とりあえず褒める・根拠のない自信ある発言は厳禁。
- 「できるか」ではなく「どうすれば実現できるか」を考える。技術的難易度を理由に提案を諦めない。
- **技術的な限界が見えたら即座に方針転換を提案する**（過去実績：トラックB＝直近加重MHの多重比較問題→解析値ベース軸への転換）。
- 会話の流れに応じて自分の役割を切り替える：機能実装＝アプリ開発者／分析設計＝データ分析設計コンサルタント／データの意味読解＝パチンコ分析専門家／手順案内＝技術サポート。
- Claude Pro機能・分析戦略についてユーザーはまだ把握していないため、Claudeから能動的に提案する（今回のClaude Code移行もその一例）。
- 1機能ずつ実装→デプロイ→実機確認（PC Chrome限定。iPhone/Claudeアプリは自動翻訳でコード破損するため厳禁）→次の実装、という順序を厳守。
- 誤った方向性はブラストが途中で修正することが多い。指摘は正しいことが多いので、指摘があれば設計方針を素直に見直す。

## 対象アプリ・リポジトリ

- **グラン本店分析アプリ** = `analysis_グラン本店.html`（大手チェーン、9月決算、店長交代は毎年4〜5月頃で設定投入傾向が変化）
- **大東洋本店分析アプリ** = `analysis_大東洋本店.html`（小型チェーン、5月決算、店長交代タイミング不明）
- 両ファイルとも同一GitHubリポジトリ `axl4415blast-hash/pachinko-tool` に保存、GitHub Pagesでホスティング
- URL（グラン本店）：`https://axl4415blast-hash.github.io/pachinko-tool/analysis_グラン本店.html`

## バックエンド

- Google Apps Script (GAS) 経由でGoogleスプレッドシートからデータ取得
- グラン本店 GAS URL: `https://script.google.com/macros/s/AKfycbzwf701DWWumTA_ImWfkC6qMw34xafwZQx2_NSJi9lD43_-sjI_6obJW3morkd_GHSE/exec`
- 大東洋本店 GAS URL: `https://script.google.com/macros/s/AKfycbwHD3mZEtoN76tL1fdl0Cml4fo3ZWLZVLfLNOagl_c1jzC4JoYAYvVXl4-9wYty5_BB/exec`
- データ収集ツール：`index.html`（パチンコ台スクリーンショットを読み取るOCRツール、Gemini API使用：`gemini-2.5-flash`が現状唯一の実用モデル）

## 両店舗の特定日・機種情報

### グラン本店
- 特定日：暦日 `1,3,9,11,13,19,21,23,27,29`。`isSpecialDay(dateStr)`関数で判定。
- **曜日軸は完全に無効・廃棄済み**（特定日はカレンダー日基準のため、曜日で見ると設定投入レジームをまたいでしまう）。
- AT機（RB確率のみで判定）：北斗転生・東京喰種・カバネリ２。`AT_MACHINES`。
- 1機種ずつ読む前提（`allData`は選択中機種のみ）。

### 大東洋本店
- 特定日：全機種 `1,7,17,27`／ジャグラー系はさらに `5,15,25`（日付末尾ベース）。
- **高設定判定バグ（G数→総回転数変換）が未解決・未診断のまま残っている**。ブラスト自身もバグの詳細を把握していない。実データを見て診断する必要がある＝最優先タスク。
- 小型チェーンは方針が揺れやすいため、読めない期間は正直に沈黙する設計にする。

## 現在の完成機能（グラン本店・実機確認済み）

1. **連続グレードスコアリング**：9つの予兆シグナルをバイナリ(true/false)から深度ベースの連続値(0-1)に変更。同率1位の横並び台数を平均4.3台→1.0台に削減。
2. **バケットプロファイルスコアリング**：各予兆のDepth範囲をK=5バケットに分割し、バックテストから実測リフトを測定。非単調ヒット率を37%→50%に改善。
3. **🎯解析値マッチページ**：機種の理論解析値（設定6相当）との適合度で台をランキング。同タイプ日の全BB/RB/回転数を合算してプール確率化。RB重み0.6・合成重み0.4（非AT機）、RB専用（AT機）。信頼性収縮あり。`localStorage`キー`high_spec_v1`。
4. **🔬予兆発見監査ページ**：機械生成30候補（5指標×3窓×2方向）から、walk-forward検証＋並べ替え検定（family-wise、400回）で「偶然の壁」を超えた予兆だけを本物と認定。多重比較の罠を回避する設計（トラックB失敗の教訓から必須化）。
5. **🎯発見狙い台（監査ページ内セクション）**：発見監査で壁を超えた予兆を、次の同タイプ対象日にforward-onlyで適用し、点灯した台をスコア順に表示。壁超え予兆0件なら正直に「出せる台がありません」と表示。信頼度（高/中/低）は答え合わせ日数と壁からの余裕から機械的に算出。

### 削除済み機能（本セッション）
- **AI分析（🤖 Anthropic Claude API連携）**：使われていなかったため完全削除。APIキー入力欄・`runAiAnalysis()`関数・関連localStorageキーすべて除去。
- **曜日ベース分析軸（「同じ曜日→次の同じ曜日」）**：ローテーション分析／直近トレンド×前回同種日の2ページから削除。両ページとも「特定日→次の特定日」固定に変更。

### 論点1（トラックB／直近加重MH）→ 中止（重要な学び）
少数サンプル（n=5〜8）の別枠検定は、緩い閾値だと偽陽性だらけ、厳しい閾値だと検出力ゼロという原理的トレードオフから逃れられないと実証済み。閾値調整では解決しない。この失敗が、後の発見監査で「並べ替え検定の壁」を必須にする根拠になった。

## 分析思想・確定原則（必読・厳守）

- **記述的であること**：可視化・エンジンとも「描写」。根拠が薄ければ空/データ不足と正直に出す。「今日は高設定だ」と断定しない。
- **投入傾向分析では指標に善悪をつけない**：BB・RB・合成・差枚・回転、すべて等しく候補として扱う。方向（ベスト/ワースト）にも善悪をつけない。「良い挙動だけを予兆にする」のは監査の意味を失わせる誤り（過去にClaude自身がこの誤りを犯した記録あり）。
- **多重比較の壁を必ず持つ**：少数サンプルで候補を多数試すと偽陽性が必ず出る。並べ替え検定/walk-forwardで「偶然かどうか」を測ってから採用。
- **プールは合計してから割る**：確率の単純平均は禁止。回転数で重み付けする。
- **過剰設計を嫌う**：自動レジーム検出は却下。境界が曖昧な問題に精密解を強制しない。判断は人間（ブラスト）が握る（手動期間指定など）。
- **統計の正しさ**：forward-only厳守（対象日より前のデータだけで特徴量を作る）。信頼性収縮は適用するが**定数はコアごとに異なる**（下記「各コアの信頼性制御方式」参照。`count/(count+5)`は全コア共通ではなく`PredictionEngine2`固有）。正規化はMantel-Haenszel層別統計。
- **バイト一致をquality gateにする**：計算コアはスタンドアロン`*_core.js`ファイルとして作り、合成データで検証してから埋め込み、diffで完全一致を確認する。（`SpecMatch`・`PredictorAudit`・`PredictionEngine2`・`computeCarryover`すべてこの方式）
- **合成データ検証を毎機能必須とする**：実データ統合前に必ず単体テストを行う。

## 技術情報

- **埋め込みコア一覧**：`PredictionEngine2`（狙い目エンジン）／`SpecMatch`（解析値マッチ）／`PredictorAudit`（発見監査、`compute`＋`picks`API）／`computeCarryover`（据え置き分析）。いずれもスタンドアロン`*_core.js`とバイト一致。
- **各コアの信頼性制御方式（実コード値・改変厳禁）**：収縮の定数は**コアごとに異なる**ので「+5共通」と誤解しないこと。
  - `PredictionEngine2`：収縮 `n/(n+5)`（＝`count/(count+5)`。この+5はこのコア固有）。
  - `SpecMatch`：収縮 `pd/(pd+3)`（`shrinkK`既定3。`pd`＝プール日数）。
  - `PredictorAudit`：**収縮定数なし**。最小サンプルゲート（点灯 `fY>=8`台日）＋family-wise並べ替え壁（`perm=400`の最大帰無リフトの95パーセンタイル）＋walk-forward（前半 `liftTrain>1`）で本物を選別する。
- **LocalStorageキー**：`gas_url` / `high_criteria_v2`（高設定基準・段階制）/ `high_spec_v1`（機種別理論解析値）/ `drift_pins_<sheetName>`。
- **高設定判定**：`isHighSetting(row, machineName?)`。段階制(tiers)＋3パターンOR。
- **大容量HTMLの扱い**：`grep -n`でランドマーク検索→該当箇所だけ`view`。全体ロードを避ける。`node --check`で構文確認後に実行。div開閉grepでバランス確認（差1が既定値）。埋め込みコアは`*_core.js`とdiffでバイト一致を確認する。

## 完了した移植（記録）

- **大東洋本店へのコア四機能移植：完了（2026-07-03）**。①連続グレード・バケットプロファイル（`PredictionEngine2`差し替え）／②🎯解析値マッチ／③🔬予兆発見監査／④🎯発見狙い台をグラン本店からバイト厳密コピーで移植。AI分析・大東洋独自4ページ（概要/時系列相関/高設定間隔/全機種比較）は削除。G数→総回転数変換バグは診断の結果**バグなし**（全6シート19,868行で検算一致）。移植中にStep3で発生したZONE_CONFIGS巻き込み削除事故を修正済み。詳細な手順・教訓は下記「移植・大規模改修プレイブック」を参照。GitHub上は`daitoyo-renewal`ブランチ（削除済み）→PR #1として`main`にマージ。

## 移植・大規模改修プレイブック（大東洋本店移植で得た教訓）

次回、別のアプリ・店舗を追加/移植する際は、着手前に以下を確認する。

- [ ] **バイト厳密コピーは機械的に抽出する**：`sed`/`tr`/`cat`でコピー元の該当範囲を抽出し、そのまま挿入する。「理解して書き直す」（要約・整形・変数名変更含む）は禁止。挿入後は抽出元・挿入先の両方から`sha256sum`を取り、完全一致を機械的に確認する（目視diffで代替しない）。
- [ ] **改行コード（CRLF/LF）を事前確認する**：コピー元とコピー先でCR/LFが異なる場合、`tr -d '\r'`で正規化してからハッシュ比較する。生バイトのまま貼ると改行混在ファイルになる。
- [ ] **大きな削除作業の前に、削除範囲の全トップレベル定義を一覧化する**：`git show <commit> -- <file> | grep -nE "^-(const |let |function |async function )"`等で削除される`const`/`let`/`function`を全て洗い出し、削除対象の機能と無関係な共有定義（ZONE_CONFIGS事故の教訓）が巻き込まれていないか確認してから実行する。
- [ ] **`node --check`は構文チェックのみで実行時エラーは検出できない**：未定義変数の参照（例：共有定義の巻き込まれ削除）は構文的には正しいコードのため検出されない。GASからの実データ取得経路（`fetchSheetData`等）を、ブラウザ実機またはCIのモックfetchで**必ず一度は実地で通す**。
- [ ] **合成データテストは「例外が出ない」で満足しない**：仕込みシグナル（良い挙動を仕込んだ台）と非仕込み台とでスコアが明確に分離するか、二値化していないか（連続値の異なり数）まで確認する。エンジンが空の合成データに対して静かにno-opしているだけ、というケースを見逃さない。
- [ ] **pre-commit hookは軽量チェックのみに絞る**：構文・div開閉バランス・コアのバイト一致など高速なものだけをコミット時に実行する。合成データによる重い単体テスト（並べ替え検定など）はGitHub Actions（push/PR時）に分離する。
- [ ] **GitHub Pagesのデプロイが「building」で5分以上停滞したら異常とみなす**：[githubstatus.com](https://www.githubstatus.com/)でGitHub側の障害有無を確認し、`POST /repos/{owner}/{repo}/actions/runs/{run_id}/rerun-failed-jobs`で該当ワークフローの失敗ジョブを再実行する。
- [ ] **Claude CodeのPreToolUseフック（`.claude/settings.json`）は「発火しないことがある」既知の不具合パターンがある**（2026-07-03判明。GitHub issue #6305, #11544 等で報告されている現象と一致）。実際にこのセッションでBashツール経由の`git commit`が2回連続でブロックされず、デバッグマーカーで検証した結果、`git commit`に限らずあらゆるBashツール呼び出しに対してフックが一切発火しないことを確認した。**確実な砦は`.git/hooks/pre-commit`（git本体のネイティブフック。core.hooksPath未設定ならデフォルトの`.git/hooks/pre-commit`が常に呼ばれる）である。** 実チェックロジックは`.claude/hooks/run-quality-checks.sh`に一本化されており、`.claude/hooks/pre-commit-check.sh`（PreToolUse用の薄いラッパー）と`.git/hooks/pre-commit`（git本体用の薄いラッパー）の両方がこれを呼ぶ。PreToolUse側の定義は「効くこともある二重の安全網」という位置づけで残してあるが、**`.git/hooks/pre-commit`はgit管理外（`.git/`配下）のためリポジトリを再clone/新規worktreeした場合は手動で再作成が必要**（内容は`.claude/hooks/run-quality-checks.sh`を呼ぶ数行のラッパーのみ）。過去の「コア四機能移植」コミット履歴を調査したが、フックが実際に発火していたことを示す直接証拠（ブロック→修正→再コミットのパターン等）はreflog上に見つからなかった（ただし拒否されたコミットはgit履歴に残らないため、この不在は「発火していた」「発火していなかった」のどちらの証明にもならない）。

## 既知の環境依存の罠

「想定通りに動かなかった」事象を時系列で一覧化する。詳細な記述は各所（プレイブック等）に残したまま、ここには「何が起きたか」と「教訓」だけを1〜2行で要約する。**新しい事象が見つかるたびにここへ追記する運用とする。**

1. **CRLFハッシュ計算方式の不一致（daymap／2026-07-04）**：`.migration-hashes/`に記録したsha256を生CRLFで計算したが、照合スクリプトはLF正規化後で計算していたため、コードは正しいのに常に不一致になった。→ **教訓：ハッシュを記録する全スクリプトで正規化方式を統一すること。**
2. **Claude Code PreToolUseフックが特定セッションで発火しない（2026-07-03）**：正しく設定してもBashツールに対してフックが一切発火しないセッションがある（GitHub issue #6305, #11544 と一致）。→ **教訓：Claude Code自体の安全機構は、git本体（`.git/hooks/pre-commit`）やCI等の独立した仕組みで必ず二重化すること。Claude Code内部の仕組みだけを信頼しない。**
3. **settings.json変更のセッション内反映タイミング不確定（2026-07-04・調査中）**：`permissions.ask`ルールを追加したが、変更したセッション内で即時反映されるか、起動時読み込みのみかが未確定。→ **教訓：設定ファイルを変更したら、新しいセッションで再起動して効果を確認する習慣を持つこと。**

## 進め方のルール（Claude Code移行後も継続）

- 1機能ずつ実装→合成データ検証→実データ統合→ブラストがPC Chromeで実機確認、の順を守る。
- 機能追加・修正のたびに、このCLAUDE.mdを更新する（セッション末、または大きな区切りごとに）。
- コミット・push前には必ず変更内容をブラストに提示し、承認を得てから実行する。
