# 引き継ぎログ｜大東洋本店アプリ：グラン本店7機能の移植 **完了報告 v1**

作成日：2026-07-05
対象ファイル：`analysis_大東洋本店.html`
参照元（読み取り専用）：`analysis_グラン本店.html`（GitHub main branch）
作業ブランチ：`daitoyo-7features`（未マージ）／PR：**#5**
前提ログ：`引き継ぎログ_大東洋本店7機能移植計画.md`（本ログはその完了版）

---

## このログの目的

グラン本店に実装済みだが大東洋本店に無かった7機能の移植が、計画（`引き継ぎログ_大東洋本店7機能移植計画.md`）どおり完了した。本ログは「何を・どの順で・どう検証して移植したか」「今回確立した自動化基盤」「発見された問題と対応」「mainマージ前に残っている作業」を、次のチャットがゼロから経緯を再構築せずに引き継げる粒度でまとめる。

**このログを開いたら、まず`CLAUDE.md`を読むこと。** 進め方の原則・既知の罠・移植プレイブックはCLAUDE.mdに集約されている。

---

## 1. 7機能移植の完了状況（コミットハッシュ・PR）

すべて`daitoyo-7features`ブランチ上。PRは **#5**（`https://github.com/axl4415blast-hash/pachinko-tool/pull/5`）。**mainには未マージ**。各Stepは「グラン本店からのバイト厳密コピー→ハッシュ照合→配線→構文/div/未定義参照/関数重複チェック→合成データ検証→ブラウザ実地確認→migration-verifier独立検証→承認→コミット→push→CI green確認→PRコメント投稿」の順で実施。

| Step | 機能 | ナビ表示 | コミット | 検証の要点 |
|---|---|---|---|---|
| 基盤 | 移植支援基盤 | — | `b762615` | git本体フック化・migrate-featureスキル・migration-verifierエージェント |
| — | CI移植ハッシュ照合 | — | `cb1fae2` | tests.ymlにcheck-migration-hashes追加 |
| 1 | 📅 日別島図 | daymap | `b37bd39` | 3状態色分け＋角台マーカー |
| 2 | 🔄 ローテーション分析 | rotation | `ef63762` | 追従型/回避型/混合型（特定日→次の特定日のみ、曜日軸なし） |
| 3 | 🧩 島の並び文脈 | adjacency | `64ee559` | 隣接リフト（固め置き/バラし/傾向なし）、cache変数含む |
| 4 | 🏷️ 店の型判定 | halltype | `e7bdbac` | z検定＋Bonferroni補正（末尾度z≥2.58＝10分母固定）。統計独立再実装で検証 |
| 5 | 📈 直近トレンド×前回同種日 | trend | `e37bad4` | S1/S2/S3バックテスト＋z検定。ヘルパーparseDiffVal含む |
| 6 | 📉 傾向ドリフト | drift | `35f2fa5` | **7機能で唯一の専用CSSブロック**（CSS+HTML+関数の3ブロック）。週次5指標＋📌localStorage |
| 7 | 🔁 特定日翌日の据え置き | carryover | `a04771b` | **7機能で最複雑**。Mantel-Haenszel層別検定（ΣA/ΣE/ΣV積み上げ）。統計独立再実装で検証 |

補助的なドキュメント/基盤コミット：`b066037`（SKILL.md Step9にPRコメント投稿を標準化）、`2993f66`（ask権限ルール・既知の罠・統計検証必須化）、`e2f42d8`（到達不可能分岐検出を全Step必須化）、`455a21d`（関数名重複検出の自動化）、`a2c6ad7`（driftFmt孤児削除）、`cabdc0c`（carryover reachesTier店舗固有調整）、`593867b`（既知の罠の根本原則追記）。

**ナビ順（大東洋・移植後16項目）**：`target / drift / suffix / daytype / zone / daymap / rotation / adjacency / halltype / trend / carryover / spec / audit / heatmap / machinetrend / settings`（グラン本店と同一順）。

---

## 2. 今回のプロジェクトで確立した自動化基盤

移植の品質を「目視」ではなく「機械的な検証」で担保するため、以下を構築した。**これらは今後の別店舗・別機能の移植でもそのまま再利用できる。**

### migrate-featureスキル（`.claude/skills/migrate-feature/SKILL.md`）
- `/migrate-feature <機能名>`で起動。計画ログの「実装手順9項目」をテンプレート化。
- 作業開始時にタスク管理ツールでStep1〜9のタスクリスト作成を必須化。
- Step1に「関数ブロックの開始点は見出しコメントとは限らない」チェックリスト（7機能の実測パターン一覧：cache変数・ヘルパー関数・定数配列・残存コメントの罠）。
- Step5に全機能結合後の関数名重複チェックを明記。
- Step7.5にmigration-verifier独立検証を必須ステップ化（飛ばして報告・コミット禁止）。
- Step8で必ず停止し承認を得るまでコミット禁止。Step9でCI成功後にPRコメント投稿。

### migration-verifierサブエージェント（`.claude/agents/migration-verifier.md`、model: opus）
- 実装の経緯を知らない第三者として、ハッシュ再計算・配線実在・構文/div/未定義参照・合成データ再現を独立にやり直す。
- **統計処理を含むStep（halltype・carryover）では、統計量（z値・lift・補正後しきい値・ΣA/ΣE/ΣV）を数式から独立に再実装し、実装側の報告値と一致することを合成データで確認する**ことを必須化（halltype・carryoverで実際に有効だった）。
- **全Step共通で、各verdict分岐の値域を数式的に検討し到達不可能な分岐が無いか確認**（trendのS3発覚を受けて追加）。
- 「問題なし」の一言禁止。行番号・数値付きで報告。

### git本体pre-commitフック（`.git/hooks/pre-commit` → `.claude/hooks/run-quality-checks.sh`）
- Claude CodeのPreToolUseフックが発火しないセッションがある不具合（既知の罠#2）への対処。**確実な砦はgit本体フック**。実チェックは`run-quality-checks.sh`に一本化し、PreToolUse用ラッパー（`pre-commit-check.sh`）とgit本体用ラッパー（`.git/hooks/pre-commit`）の両方がこれを呼ぶ。
- **注意：`.git/hooks/pre-commit`はgit管理外**。再clone/新規worktree時は手動再作成が必要（内容は`run-quality-checks.sh`を呼ぶ数行）。
- run-quality-checks.sh のチェック内容：①構文（node --check相当）②div開閉バランス③計算コアのバイト一致（PredictionEngine2/SpecMatch/PredictorAudit）④未定義参照⑤移植ハッシュ照合⑥関数名重複。

### CI移植ハッシュ照合＋関数重複検出（`.github/workflows/tests.yml`）
- push/PR時に`check-migration-hashes.js`と`check-function-duplicates.js`を実行。ローカルフックを`--no-verify`で迂回してもCIで最終検出。
- mainブランチ保護（`required_status_checks: test`／`enforce_admins: true`）が既に有効なため、同じtestジョブに追加するだけで迂回不可能な最終防衛線になる。

### check-migration-hashes.js（`.migration-hashes/<機能名>.txt`）
- 各機能のブロックを「start_marker＋行数＋LF正規化sha256」で記録し、大東洋側の現物と照合。**マーカー文字列で位置を探す**ため他機能追加で行番号がズレても機能する。
- `[css]`/`[html]`/`[func]`など任意セクション名に対応（driftの3ブロック構成で[css]を実データ検証済み）。
- **記録するsha256は必ずCRLF→LF正規化後で計算**（生CRLFで記録すると常に不一致＝既知の罠#1）。

### check-function-duplicates.js
- グラン本店を「動的な許可リスト」として使い、大東洋で「2回以上（実際の重複）かつグラン超過」のfunction名を検出。IIFEコア内スコープヘルパー（addDays/compute/fmt/num/pDate等、グランにも同数）と大東洋固有の単発関数（isJugglerMachine等）は誤検出しない。ネガティブテストで失敗を確認済み。

### check-html-id-duplicates.js（carryover HTML誤抽出事故を受けて追加）
- `analysis_大東洋本店.html`のid属性を全抽出し、2回以上出現するidをコミット時にexit=1で検出（該当id名・全出現行番号を出力）。HTMLのidはページ内で一意であるべきというW3C仕様に基づく。**ページブロックの範囲を誤って別ページまで巻き込み二重化する事故（下記3(e)）を機械検出する。** function重複検出のHTML版。`run-quality-checks.sh`（チェック7）＋`tests.yml`に組込み、ネガティブテスト済み。

---

## 3. 発見された重要な問題とその対応

### (a) driftFmt孤児（前回移植の巻き込み）— コミット`a2c6ad7`
- drift移植後の全機能結合後 function名重複チェックで、大東洋にだけ`driftFmt`が2つあることを発見。
- 由来：**Step2（`357b260`＝前回のコア四機能移植）でPredictionEngine2コアをコピーした際、無関係なdriftFmt行が単独で紛れ込んでいた**（当時driftの他関数は未存在）。今回のdrift移植で正規版が入り二重定義に。
- 両定義はバイト同一・トップレベル（グローバル）→後者が前者を無言で上書き。実害はないがfunction宣言の無言上書きという汚れ。
- 対応：PredictionEngine2抽出範囲（2002-2254行）の外側にあることを確認し、孤児のみ削除。独立コミット。**この発見を受けてcheck-function-duplicates.jsを自動化基盤に格上げ**。

### (b) trend「逆効果」判定の到達不可能分岐 — CLAUDE.md「既知のロジック上の課題」に記録
- trendのS3判定で、`significant=z≥1.96`（プラス方向限定）と「逆効果」分岐の`lift≤0.8`（逆方向）は数学的に同時成立不可能（`z≥1.96`⟹`fRate>nRate`⟹`lift>1`）。「逆効果（むしろ避ける）」のUI表示は**恒久的に到達不可能な死んだコード**。
- これは**グラン本店原本の仕様**であり、バイト同一移植のため大東洋では変更しない（原本に忠実であるべき移植原則を優先）。
- グラン本店側の修正要否は本プロジェクトのスコープ外としてユーザー判断待ち。**この発見を受けて、migration-verifierに全Step共通の到達不可能分岐チェックを追加**。
- 既存6機能の追加チェック結果（Task#8）：**trend以外に到達不可能分岐なし**。daymap（判定ロジック無し）／rotation（判定不可/追従/回避/混合、全到達可）／adjacency（データ不足/固め置き/バラし/傾向なし、全到達可）／halltype（角系/末尾系はlift条件とz有意条件が**同方向**なのでS3のような相反なし、全到達可）／drift（driftMovement/driftDiagnoseは`Math.abs`で両方向到達可）。

### (c) carryover reachesTierの店舗固有調整 — コミット`cabdc0c`
- 大東洋の`criteria`は`{rotation:5000, gosei:125, rb:280}`のフラット構造で`tiers`を持たないため、`carryoverReachesTier`は常にフォールバック枝`rotation>0`を通る。これは「ゼロ稼働のみ除外」で緩すぎ、翌日1〜99回転の薄い台まで判定可能に含めていた。
- 対応：バイト同一ベース版（`a04771b`）を先にコミットした上で、**大東洋固有の意図的逸脱**として`rotation>0`→`rotation>=100`に変更（`cabdc0c`、独立コミット）。合成データでrot=50の判定が変更前true→変更後falseになることを確認。
- migration-verifierで**computeCarryover本体（DIコア）がバイト同一のまま**（`698a5875...`一致）＝改変がcarryoverReachesTier関数内に完全に閉じていることを独立確認。ハッシュ記録（carryover.txt func：345→349行、新sha256）も逸脱の旨を明記して更新。

### (d) CSS専用ブロックの見落としリスク — driftで顕在化
- driftは7機能で唯一`<style>`内に専用CSSブロック（`.diag-banner`・`.drift-*`計17クラス）を持つ。当初2ブロック前提だったが、範囲確定時に第3のCSSブロックの存在に気づき正しく移植。
- Task#8のCSS照合結果：**専用CSSブロックを持つのはdriftのみ**（グラン`<style>`内の機能別見出しコメントは`/* ── 傾向ドリフト ── */`の1件のみ、daymap/rotation/adjacency/halltype/trendは`.<feature>-*`クラス0件＝base classのみ使用）。大東洋側もdrift CSS 17クラス＋`.diag-banner`が正しく存在。**carryoverはdriftの`.diag-banner`を流用**（drift先行移植の順序が正しかった）。

### (e) 【最重要】carryover HTML誤抽出によるページ二重化事故 — コミット`1200e00`（修正）／`4ee67cb`（再発防止）
本プロジェクトで最も重大な事故。**「carryover完了・全検証合格」と一度報告した後、ブラスト（ユーザー）のPC Chrome実機確認中にブラウザのIssuesパネル（Duplicate form field id）で発覚した。**

- **原因**：Step7でcarryoverのHTMLブロックをグラン本店465-542行（78行）として抽出したが、`page-carryover` div は実際には**465-473行（9行）**で閉じており、その後ろに続く`page-heatmap`（476-503行）・`page-machinetrend`（505-542行）まで巻き込んでいた。carryoverページはUIの5ブロックをJSが`carryover-body`へ動的描画する設計で、**静的HTMLが`<div id="carryover-body"></div>`という空コンテナだけ＝極端に短い**ため、終端を`page-spec`直前まで（78行）と見誤った。
- **実害**：大東洋には既にheatmap/machinetrendページが存在したため二重化。`getElementById`は文書順の先頭要素のみ返すため、誤挿入したグラン版コピー（先頭）がアクティブになり、元の大東洋版（後方）が死蔵。① heatmapで大東洋固有の「特定日（ジャグラー限定）のみ」フィルタが欠落、② machinetrendでグラン版の誤った日付タブ（末尾1/3/9＝1,3,9,11…日）が表示され大東洋の正しいタブ（特定日1,7,17,27／5,15,25日）が使えない。**画面上は両ページが「描画されている」ため一見動いて見えるが、中身が大東洋仕様でなくグラン仕様**という、目視では気づきにくい実害。
- **既存の自動検証がすべてすり抜けた（本事故の最大の教訓）**：
  - **移植ハッシュ照合（check-migration-hashes.js）**：記録ハッシュ自体が誤抽出（78行）から計算されていたため、大東洋の現物と一致してMATCH（garbage-in-garbage-out）。「大東洋が記録値と一致」は保証しても「抽出範囲が正しい」は保証しない。
  - **関数名重複検出（check-function-duplicates.js）**：二重化したのはHTMLページdivであり関数ではないため対象外。
  - **div開閉バランス**：3ページとも完結しているのでバランスは崩れず素通り。
  - **migration-verifier（当時）**：「大東洋ブロックがグラン465-542とバイト一致・page-spec非巻き込み」は真として合格。範囲を「carryover HTML 465-542」と提示されたため、その範囲が3ページをまたいでいることを問わなかった。
  - **後から追加したマーカー重複検出（4db53f6）**：heatmap/machinetrendは`.migration-hashes`の追跡マーカー対象外なのでカバーせず。
  - → **結局、ブラウザのIssuesパネル（重複id）というブラスト側の一次情報だけが検出器だった。** これは「根本原則（Claudeはユーザー画面を観測できない）」の裏返しで、逆にユーザーの実機確認が最後の砦になった事例。
- **修正（`1200e00`）**：誤挿入した重複2ページ（71行）を削除し、carryover HTMLブロックを正しい9行（page-carryoverのみ）に修正。`.migration-hashes/carryover.txt`の[html]を`lines=78→9`・新sha256`0b9ade7c...`へ更新。carryoverページ自体・関数ブロックはバイト同一のまま。migration-verifierが**検証者自身でグラン本店page-carryover divの開閉ネストを1つずつ数え、9行で閉じ・当初78行が3ページをまたいでいたことを独立裏付け**。実機でid重複0・大東洋固有UI復元・carryover機能無傷を確認。
- **再発防止＝本プロジェクトの最終成果物（`4ee67cb`）**：この事故から、以下2つを恒久的な防御として確立した。
  1. **`check-html-id-duplicates.js`（HTML id重複チェック）**：`analysis_大東洋本店.html`のid属性を全抽出し、2回以上出現するidをコミット時にexit=1で機械検出（該当id名・全出現行番号を出力）。`run-quality-checks.sh`（ローカルgitフック）＋`tests.yml`（CI）の両方に組込み、ネガティブテストで検出を確認済み。**今回のようなページブロック範囲誤りによる二重化を、以後はコミットの瞬間に自動で捕捉する。** check-function-duplicates.js（function重複）と対になる「HTML版の無言上書き検出」。
  2. **div独立追跡ルール（migration-verifier.md）**：「HTMLページブロックの範囲確定では、対象`id="page-X"`の開始divと対応する閉じdivを、検証者自身がネストを1つずつ数えて独立にたどること」を恒久ルール化。特にJS動的描画依存で静的HTMLが短い場合（carryoverのような空コンテナ設計）の終端誤認に注意することを明記。SKILL.mdにも全機能結合後のid重複チェックを追記。

---

## 4. 既知の環境依存の罠（CLAUDE.mdより。根本原則含む）

**【根本原則】Claude Codeは、ユーザーのターミナル画面表示・ユーザー本人の操作・課金を伴う起動判断を、内部から観測・実行することが構造的にできない**（`/agents`・`/permissions`等のTUIコマンド、権限モード・使用モデルの自己確認、画面ダイアログの読み取り、`/code-review ultra`等の課金コマンドの起動、すべてこの1制約から派生）。同種の「私からは確認できません」に遭遇したら、個別調査せず「ユーザー側での確認をお願いする」形で速やかに対応する。以下の個別事例はいずれもこの根本原則の現れ。

1. **CRLFハッシュ計算方式の不一致（daymap）** → ハッシュ記録の全スクリプトで正規化方式を統一。
2. **Claude Code PreToolUseフックが特定セッションで発火しない**（GitHub issue #6305/#11544）→ git本体フックやCIで必ず二重化。Claude Code内部の仕組みだけを信頼しない。
3. **【解決済み】settings.json変更のセッション内反映タイミング** → allow競合解消後、自動モードのテストコミットで確認ダイアログ表示を実地確認。
4. **`git ls-files <path>`は未追跡ファイルでも終了コード0** → 追跡状態は`git ls-files -s`の出力有無や`git status`で確認。`&&`の成否だけで判断しない。
5. **【解決済み】`settings.local.json`の広いgit系allowルールがaskルールと競合** → askルール追加時はlocal.jsonの競合allowを必ず確認。理論でなく実地テストで確定。

---

## 5. 未完了タスク・mainマージ前に残っている作業

### プロジェクト内タスク
- **すべて完了**。Task#8（既存6機能の到達不可能分岐チェック＋専用CSSブロック照合）も完了し、追加の是正事項は無し（trend S3の既知課題以外に到達不可能分岐なし／CSSブロックはdriftのみで正しく移植済み）。
- **【重要】carryover HTML誤抽出事故（上記3(e)）も修正完了**：実機確認で発覚後、重複2ページ削除・id重複チェック自動化・div独立追跡ルール恒久化まで完了（`1200e00`／`4ee67cb`）。CI green。**この事故は「既存の自動検証がすべてすり抜け、ユーザーの実機確認だけが検出器だった」という本プロジェクト最大の教訓であり、その再発防止策（HTML id重複チェック・div独立追跡ルール）が本プロジェクトの最終成果物である。**

### mainマージ前に残っている作業
- **ブラスト（ユーザー）によるPC Chrome実機での最終確認**：本ログ時点でブラウザ検証はClaude Codeのpreview toolで実施済みだが、CLAUDE.mdの原則に従い、実データ（GAS経由）での最終確認はブラストが行う。特に**ジャグラー系機種で特定日5・15・25が正しく特定日扱いされること**、**小型機種で各機能が「データ不足」を正直に返すこと**を目視確認。
- **最終レビュー（ultrareview / `/code-review ultra`）**：課金を伴い、**ユーザーがターミナルで起動する必要がある**（Claude Codeからは起動不可＝根本原則）。ブランチ差分全体（16コミット）を対象に実施を推奨。
- **PR #5のマージ**：上記確認後。マージ後にGitHub Pagesへの反映を`last-modified`ヘッダー等の一次情報で確認（「building」5分停滞は異常＝プレイブック参照）。
- **`.git/hooks/pre-commit`の再作成**：マージ後に別環境で作業する場合、git管理外のため手動再作成が必要。

### スコープ外（ユーザー判断待ち）
- **trend「逆効果」判定の到達不可能分岐のグラン本店側修正要否**（本プロジェクトはバイト同一移植のため大東洋では変更しない）。

---

## 6. 大東洋本店固有の設計判断

移植は原則バイト同一だが、大東洋は独立アプリであり以下の固有仕様を持つ。7機能はこれらを`isSpecialDay()`/`isHighSetting()`/`getZones()`等のヘルパー経由で呼ぶだけなので**コード自体はグランと同一**だが、実データ確認時はこれらの差異を意識すること。

- **特定日定義**：大東洋＝全機種`1,7,17,27`／ジャグラー系はさらに`5,15,25`（日付末尾ベース）。グラン＝`1,3,9,11,13,19,21,23,27,29`（暦日）。`isSpecialDay()`は大東洋側に正しく実装済み（ジャグラー分岐あり）。特定日が少ないぶん、carryover/halltype等で「データ不足」表示が出やすいが設計どおりの正直な挙動。
- **高設定判定モデル（criteria構造）が根本的に異なる**：
  - 大東洋＝**フラット単一しきい値**。`criteria = {rotation:5000, gosei:125, rb:280}`（`tiers`キー無し）。非AT機は`rotation≥5000 かつ gosei≤125 かつ rb≤280`のAND判定。AT機（`AT_MACHINES`：L東京喰種・カバネリ・北斗転生2・モンハンライズ・L北斗）は機種別の専用計算式。localStorageキーは`high_criteria_v2_daito`（グランと分離）。
  - グラン＝**段階制（tiers）**。`criteria = {tiers:[...]}`で各tierが`condBoth/condGosei/condRb`の3パターンOR。
  - この違いにより、carryoverの`carryoverReachesTier`が大東洋では常にフォールバック枝を通る（→上記3(c)の店舗固有調整の背景）。
- **G数→総回転数変換は診断済み・バグなし**（2026-07-03、コア四機能移植時に全6シート19,868行で検算一致）。以前CLAUDE.mdにあった「未解決の最優先タスク」は古い記述で、今回訂正済み。
- **AT機の扱い**：大東洋独自の`AT_MACHINES`と専用計算式を持つ。7機能側はAT機を特別扱いしないため影響なし。
- **ゾーン実装**：大東洋は`ZONE_CONFIGS`＋`getCurrentZones()`/`getCurrentCorners()`。7機能は`getZones()`/`getCornerMachines()`経由で呼ぶだけで、大東洋固有のゾーン実装には触れない。

---

## 次のチャットでの開始手順

1. `CLAUDE.md`と本ログを読む。
2. PR #5（`daitoyo-7features`）の状態を確認（`gh pr view 5` 等。ghは導入済み・要認証）。
3. ブラストのPC Chrome実機確認結果を待つ／促す。
4. ブラスト起動のultrareviewの結果があれば対応。
5. 問題なければPR #5をmainにマージ→GitHub Pages反映確認。
6. マージ後、CLAUDE.mdの「完了した移植」に本7機能移植を追記し、本ログを完了記録として残す。
