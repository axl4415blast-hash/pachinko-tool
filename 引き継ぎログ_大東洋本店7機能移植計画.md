# 引き継ぎログ｜大東洋本店アプリ：グラン本店7機能の移植計画

作成日：2026-07-03
対象ファイル：`analysis_大東洋本店.html`
参照元（読み取り専用）：`analysis_グラン本店.html`（GitHub main branch の現在の状態）

---

## このログの目的

グラン本店には実装済みだが大東洋本店にはまだ無い7機能を移植する。前回の「コア四機能移植（PredictionEngine2/SpecMatch/PredictorAudit/発見狙い台）」と同じ品質基準・同じ作業手順で進める。このログと`CLAUDE.md`を読めば、次のチャット（Claude Code）がゼロから経緯を再構築せずに着手できることを目指す。

**このログを開いたら、まず`CLAUDE.md`を読むこと。** CLAUDE.mdには進め方の原則・完了済み機能・移植プレイブックが集約されている。このログはその上に乗る「今回固有の作業内容」である。

---

## ⚠️ 最重要の前提：コピー元は必ずGitHub上の現在のファイルを使うこと

このログの元になった引き継ぎログ（無印〜v6）には、**現在は既に廃止された古い仕様**が記述されている箇所がある。具体的には：

- v2・v3ログの「①ローテーション分析」「④直近トレンド×前回同種日」には「日の基準：特定日／同じ曜日」という曜日セレクタの記述があるが、**この曜日軸は既に削除済み**（本プロジェクトの別セッションで、多重比較の罠を避けるため特定日基準のみに統一した）。
- ログ本文には「AI分析セクションは保持」という記述もあるが、**AI分析機能自体が既に削除済み**。

**したがって、7機能の実装コードを読み取る際は、ログの記述ではなく、必ず`analysis_グラン本店.html`の実際のコード（現在のGitHub main branch）を一次情報として使うこと。** ログは「なぜその設計にしたか」という背景・統計的根拠を理解するために読み、コードそのものは現物からコピーする。

**実行前の確認コマンド（必須）：**
```
grep -c "AI分析\|runAiAnalysis" analysis_グラン本店.html   # 0であること
grep -c "同じ曜日" analysis_グラン本店.html                # 0であること
wc -l analysis_グラン本店.html                             # 4031行前後であること
```
これが崩れていたら、リポジトリが古い状態の可能性があるので作業前に`git pull`を行うこと。

---

## 対象7機能（現状の差分・確定事実）

大東洋本店の現在のナビゲーション（9項目）：
`target / suffix / daytype / zone / spec / audit / heatmap / machinetrend / settings`

グラン本店の現在のナビゲーション（16項目）。太字が今回移植する7機能：
`target / **drift** / suffix / daytype / zone / **daymap** / **rotation** / **adjacency** / **halltype** / **trend** / **carryover** / spec / audit / heatmap / machinetrend / settings`

7機能は互いに強い依存関係を持たない（後述の各機能仕様を参照）。よって「1機能ずつ実装→検証→承認→次」という、これまでのプレイブック通りの順序で進められる。

---

## 実装順序（推奨）

複雑さ・リスクが低い順に並べた。理由は各Stepの冒頭に記載。

| Step | 機能 | 複雑度 |
|---|---|---|
| 1 | 📅 日別島図（daymap） | 低（統計処理なし、表示のみ） |
| 2 | 🔄 ローテーション分析（rotation） | 低〜中 |
| 3 | 🧩 島の並び文脈（adjacency） | 中 |
| 4 | 🏷️ 店の型判定（halltype） | 中（多重比較補正あり） |
| 5 | 📈 直近トレンド×前回同種日（trend） | 中〜高（バックテスト＋z検定） |
| 6 | 📉 傾向ドリフト（drift） | 中〜高（週次5指標＋UI状態管理） |
| 7 | 🔁 特定日翌日の据え置き（carryover） | 高（MH正規化＋層別＋ズラし計算） |

---

## 各機能の詳細仕様

### 共通で使う既存ヘルパー（大東洋本店に既に存在すること確認済み）

以下はすべて大東洋本店の現在のコードに存在する。7機能はこれらを無改変で呼び出すだけでよい。

- `isHighSetting(row, machineName?)` — 高設定判定
- `isSpecialDay(dateStr)` — 特定日判定（大東洋固有：全機種1,7,17,27／ジャグラー系は5,15,25追加）
- `getZones()` → 内部で`getCurrentZones()`を呼ぶ（大東洋403行付近で定義済み）
- `getCornerMachines()` → 内部で`getCurrentCorners()`を呼ぶ
- `criteria`（グローバル変数。現在シートの高設定基準。carryoverReachesTierが参照）
- `parseLocalDate`, `WEEKDAY_NAMES`, `getWeekday`, `parseFraction`

**移植時の注意**：グラン本店側のコードは`getZones`/`getCornerMachines`を直接呼んでいるだけで、大東洋固有のゾーン実装（`getCurrentZones`/`getCurrentCorners`、ZONE_CONFIGS）には一切触れない。つまり**7機能のコード自体はグラン・大東洋で完全に同一であるべき**（バイト厳密コピー可能）。大東洋固有の分岐は不要。

---

### Step1：📅 日別島図（daymap）

**グラン本店での実装場所**：
- ページHTML：`id="page-daymap"`（215行付近〜）
- 関数：`renderDayMap()`（3178行）、`renderDayMapBody(targetDate, ZONES, CORNER_MACHINES)`（3208行）

**仕様**：日付ドロップダウンで選んだ1日の全台を、ZONE_CONFIGSの物理配置（島）順に並べ、高設定だった台を赤、通常を灰色、データなしを点線枠で表示する。特定日には「【特定日】」タグを付ける。角台には枠を強調表示。

**統計処理なし**（1日分の実データをそのまま可視化するだけ）。7機能の中で最もリスクが低い。まずここから着手し、Claude Codeでの新フェーズの作業パターン（バイト厳密コピー→ハッシュ確認→実データ確認）を1周させて確認するのに適している。

**依存**：`getZones()`, `getCornerMachines()`, `isSpecialDay()`, `isHighSetting()`, `WEEKDAY_NAMES`, `getWeekday()` のみ。外部キャッシュ等への依存なし。

---

### Step2：🔄 ローテーション分析（rotation）

**グラン本店での実装場所**：
- ページHTML：`id="page-rotation"`（356行付近〜）
- 関数：`renderRotation()`（3276行）、`renderRotationBody()`（3281行）

**仕様**：前回の特定日に高設定だった「台番号」「末尾」が、次の特定日にも高設定になるか（追従率）を集計。追従率60%以上＝追従型、30%以下＝回避型、中間＝混合型と判定。台番号ベース・末尾ベース、2つの遷移テーブルを表示。

**重要**：曜日基準（同じ曜日→次の同じ曜日）は**実装しない**。既に廃止済みの仕様。特定日→次の特定日のみ。

**依存**：`isSpecialDay()`, `isHighSetting()`, `getCornerMachines()`のみ。

---

### Step3：🧩 島の並び文脈（adjacency）

**グラン本店での実装場所**：
- ページHTML：`id="page-adjacency"`（385行付近〜）
- 関数：`renderAdjacency()`（3409行）

**仕様**：同じ列で物理的に隣り合う台（`getZones()`のmachines配列で±1）が、同じ日に両方高設定だったかを隣接ペア単位（双方向）で集計。
- 指標：隣接リフト = A÷B。A＝高設定台の隣も高設定だった率、B＝非高設定台の隣が高設定だった率。
- 1.3倍以上＝固め置き、0.7倍以下＝バラし、中間＝傾向なし。
- 「連（れん）」テーブル：同日に2台以上連続で高設定だった並びを新しい順に列挙。

**注意書きとして表示すべき文言**：「これは同日内の置き方のクセであり、翌日予測ではない」（グラン本店の実装に既にこの注記が入っている。移植時も維持すること）。

**依存**：`getZones()`, `getCornerMachines()`, `isHighSetting()`のみ。

---

### Step4：🏷️ 店の型判定（halltype）

**グラン本店での実装場所**：
- ページHTML：`id="page-halltype"`（404行付近〜）
- 関数：`renderHalltype()`（3557行）

**仕様**：機種ごとに「角系／末尾系／単品系」を診断。対象日フィルタ（特定日／通常日／全期間、初期値=特定日）付き。
- **角度** = 角台高設定率 ÷ 非角台高設定率。z検定で有意（z≥1.96）かつ1.5倍以上で「角系」。
- **末尾度** = 最も高い末尾の率 ÷ **非角台平均**（角台の影響を混入させないため非角台のみで計算）。**Bonferroni補正したz≥2.58**かつ1.8倍以上で「末尾系」。
- **単品度** = 角度・末尾度どちらも非該当かつ1日あたりの高設定台数が少ない場合。

**⚠️統計上の注意点（過去に実際に踏んだ罠。移植時も同じ考慮が必要）**：
1. 角台の効果が末尾度の計算に漏れ込む問題 → 末尾度は必ず非角台データのみで計算する
2. 末尾は約10種類あるため、単純なz検定だと多重比較で偶然の最大値を「有意」と誤判定する問題 → 末尾度の検定には必ずBonferroni補正を適用する

**依存**：`getZones()`, `getCornerMachines()`, `isHighSetting()`のみ。

---

### Step5：📈 直近トレンド×前回同種日（trend）

**グラン本店での実装場所**：
- ページHTML：`id="page-trend"`（446行付近〜）
- 関数：`renderTrend()`（3733行）、`renderTrendBody()`（3738行）

**仕様**：前日以前の情報だけを使う3つの予兆をバックテストし、統計的に有効と確認できた予兆だけで次の狙い目候補を出す。
- **S1 前回もアツい**：前回の特定日に高設定だった台
- **S2 最近アツい**：直近3カレンダー日で1回でも高設定だった台
- **S3 最近ボロ負け**：直近3日の差枚合計が機種内ワースト30%の台
- パネルA（バックテスト）：各予兆の該当台高設定率 vs 非該当を倍率(lift)・2標本z検定で評価。lift≥1.2かつz≥1.96で「本物」と判定、それ以外は「効果なし（思い込み）」と正直に表示。
- パネルB（次の狙い目候補）：「本物」と判定された予兆にのみ基づき候補を提示。本物がゼロなら「信頼できる予兆なし」と表示。

**重要**：ここも曜日基準は**実装しない**（既に廃止済み。特定日→次の特定日のみ）。

**依存**：`isSpecialDay()`, `isHighSetting()`, `parseFraction`, 差枚パース用の内部ヘルパー（`parseDiffVal`相当。グラン本店側の実装をそのまま確認すること）のみ。

---

### Step6：📉 傾向ドリフト（drift）

**グラン本店での実装場所**：
- ページHTML：`id="page-drift"`（215行付近〜）
- 関数群（すべて`drift`プレフィクス。1255〜1421行付近に集中）：
  - `DRIFT_METRICS`（1255行、5指標の定義配列）
  - `computeWeeklyDrift(data)`（1269行）：週次5指標を計算
  - `driftNum` / `driftFmt` / `driftWeekStart` / `driftNextDay`（1263〜1266行、日付ヘルパー）
  - `driftSmooth` / `driftSeries` / `driftMovement`（1322〜1336行）
  - `driftPinKey` / `driftGetPins` / `driftSetPins`（1337〜1339行、localStorage永続化）
  - `driftCardHtml` / `driftDrawChart` / `driftDiagnose`（1341〜1385行）
  - `renderDrift()`（1386行）

**仕様**：店のクセの推移を週次で5指標、折れ線グラフで可視化する（予測ではなく記述。店長交代のような「方針の変化」をユーザー自身が目で見て答え合わせするための画面）。
1. 全体高設定率（%）
2. 特定日−通常日の差（pt、基準線0）
3. 角台リフト＝角台率÷非角台率（倍率、基準線1.0）
4. 末尾リフト＝非角台内の最強末尾÷平均（倍率、基準線1.0）
5. 特定日高設定→翌日も高設定の据え置き率（%）

**設計上の重要ルール**：
- 稼働台の定義＝その週の総回転数が100以上の台のみ集計対象
- 薄い週は正直にnull（率系:母数<8、lift系:片側<4、据え置き系:ペア<4）→グラフはその週だけ線を切る（`spanGaps:false`）
- 5指標はフラット表示（主役/補助の固定順位は無い）。各グラフに「直近の動き」チップ（直近3週 vs その前3週の生値比較、閾値を超えたら「動きあり↑↓」、未満なら「横ばい」、有効週<6なら「データ不足」）
- 📌ボタンで指標を上部に固定。**localStorageキーは`drift_pins_<sheetName>`という店ごと・機種ごとのキー**（大東洋本店移植時は自動的に大東洋のsheetNameで別キーになるので、グラン本店とデータが混ざる心配はない）
- 「3週移動平均で均す」トグルあり
- 一言診断（`driftDiagnose`）：全体率のrecent3 vs before3で安定/変化を1文、特定日−通常日差でメリハリの有無を付記

**依存**：`isHighSetting()`, `isSpecialDay()`, `getCornerMachines()`, `parseLocalDate`のみ。

---

### Step7：🔁 特定日翌日の据え置き（carryover）

**グラン本店での実装場所**：
- ページHTML：`id="page-carryover"`（466行付近〜）
- 関数群（1864〜2110行付近）：
  - `carryoverNextDay(ds)`（1868行）
  - `carryoverReachesTier(row)`（1872行）：`criteria.tiers`を参照して「翌日の回転数が最低段階に届くか」を判定
  - `carryoverNeighborsOf(no)`（1880行）：`getZones()`のmachines配列から前後の台番号を取得
  - `computeCarryover(data, deps, opts)`（1894行）：計算コア本体。依存性注入方式（`deps`オブジェクトで`isHigh`/`reachesTier`/`isSpecial`/`nextDate`/`diffOf`/`machineNo`/`neighborsOf`を渡す）
  - `renderCarryover()`（2092行）

**仕様（7機能で最も複雑。5ブロック構成）**：
1. 結論バナー：据え置きが統計的に有効かを1文で
2. 据え置き率 vs 翌日ベースライン：**Mantel-Haenszel層別検定**（翌日ごとに全体高設定率で条件付け。予測エンジン=PredictionEngine2と同じ正規化手法）。lift・z・有意判定を表示
3. 差枚3分割（上位/中位/下位）：据え置かれた台が「好調で終えた台」か「凹んで終えた台」かの層別据え置き率
4. ズラし分析：据え置かれなかった台の隣が翌日新規に高設定になったか。ランダム期待値`1-(1-p_new)^隣数`と比較し、上回った場合のみ「横ズラし傾向あり」と記述（有意性は主張しない）
5. 翌日据え置き狙い候補台：直近特定日の高設定台を、差枚層＋その層の据え置き率つきでリスト

**正直さの設計**：翌日の回転数が最低段階に届かない台は「判定不可」として分母から除外し、件数を明示する。データが薄い場合（有効ペア<4 または判定可能台<8）は率を出さず沈黙する。

**依存**：`isHighSetting()`, `isSpecialDay()`, `getZones()`, `criteria`（`carryoverReachesTier`が参照）。carryoverは`computeCarryover`が依存性注入(`deps`)方式になっているため、**関数本体はグラン・大東洋で完全にバイト同一にできる**（配線部分だけが呼び出し側で変わる）。これはPredictorAuditと同じパターンなので、Step5(大東洋への発見監査移植)のときと同じ手順がそのまま使える。

---

## 実装手順（1機能ごとに以下を繰り返す。CLAUDE.mdの移植プレイブークに準拠）

各Stepで以下を順番に実行する。

1. **プランモードで範囲を確定**：グラン本店から抽出するHTML範囲・関数範囲・nav-item・showPage分岐の行番号を、`grep -n`で実際に確認する（本ログの行番号は執筆時点の参考値。必ず現物で再確認すること。前回の移植作業でも、作業を進めるうちに行番号はずれていった）。
2. **バイト厳密コピー**：`sed`/`head`/`tail`/`cat`で行範囲を抽出。改行コードを確認し、必要ならLF正規化（`tr -d '\r'`）。**理解して書き直すことは禁止**（過去のZONE_CONFIGS巻き込まれ削除事故の教訓：範囲の中に無関係な既存コードが挟まっていないか、抽出前に必ず目視確認する）。
3. **sha256ハッシュで機械的な一致確認**：抽出元と挿入後の再抽出分のハッシュが完全一致することを確認。目視diffに頼らない。
4. **配線**：`nav-item`追加、`showPage('xxx',this)`ボタン、`renderPage`の分岐（`else if (id==='xxx') renderXxx();`）を追加。
5. **構文・構造チェック**：`node --check`、div開閉バランス、既存の未定義参照チェックスクリプト（`.claude/hooks/check-undefined-refs.js`）を実行。
6. **合成データ検証**：仕込んだシグナルと非仕込みでスコア・判定が明確に分離することを確認（例外が出ないだけでは合格にしない）。特にStep4（halltype）はBonferroni補正の効果を、Step7（carryover）はMH正規化と層別集計の効果を、それぞれ合成データで裏付けること。
7. **実データ経路の実地確認**：実際にGASからデータを取得し、該当ページをブラウザで開いて描画・コンソールエラー無しを確認する（`node --check`だけでは検出できないバグがあることは、大東洋本店移植のZONE_CONFIGS事故で実証済み）。
8. **報告・承認待ち**：git diff --stat、grep残存確認、node --check結果、実データ確認結果をまとめてブラスト（チャット側）に提示し、承認を得てからコミット。
9. **push・PR反映確認**：pushしてCIがgreenであることを確認。GitHub Pagesへの反映も、`last-modified`ヘッダーなど一次情報で確認する（ステータス文字列だけを信じない。過去にビルド失敗を「ビルド中」と誤認した事故があった）。

**ブランチ運用**：前回同様、作業用ブランチ（例：`daitoyo-7features`）を作成し、`main`には直接コミットしない。7機能すべて完了・実機確認OKが出てからマージする。

---

## 各Stepの検証観点（早見表）

| Step | 機能 | 合成データで必ず確認すること |
|---|---|---|
| 1 | daymap | 高設定/通常/データなしの3状態が正しく色分けされる。角台マーカーが出る |
| 2 | rotation | 追従率60%以上→追従型、30%以下→回避型と正しく判定される |
| 3 | adjacency | 隣接リフトが1.3以上/0.7以下/中間で正しく判定分岐する |
| 4 | halltype | 角台のみに信号を仕込んだ場合に角系と判定される。末尾の多重比較でBonferroni補正が効いていることを、補正なし版と比較して確認する |
| 5 | trend | 3予兆のうち仕込んだものだけが「本物」と判定され、仕込んでいないものは「効果なし」と判定される |
| 6 | drift | 週の途中で仕込んだシグナル変化（店長交代相当）が、5指標のいずれかで段差として検出される。薄い週でnullが正しく出る |
| 7 | carryover | MH正規化により、単純な据え置き率の比較では見えない差が正しく検出される。判定不可台が正しく分母から除外される |

---

## 大東洋本店固有の考慮事項（Step6・7で特に重要）

- **特定日の定義が違う**：大東洋は「全機種1,7,17,27／ジャグラー系はさらに5,15,25」。グラン本店は「1,3,9,11,13,19,21,23,27,29」。7機能はすべて`isSpecialDay()`をdeps経由/直接呼び出しで使うだけなので、**コード自体の変更は不要**。ただし実データ確認の際は、必ずジャグラー系機種で5・15・25が正しく特定日扱いされることを目視確認すること（大東洋本店移植Step6で行った確認と同じ）。
- **AT機の扱い**：大東洋本店は独自の`AT_MACHINES`と専用計算式を持つ（`analysis_大東洋本店.html`内に既存）。7機能側はAT機を特別扱いするロジックを持たないため、影響なし。
- **`criteria`変数の初期値**：大東洋は`{ rotation: 5000, gosei: 125, rb: 280 }`。carryoverReachesTierはこの構造を使うため問題なし。

---

## CLAUDE.mdとの関係

このログの完了後、CLAUDE.mdの「次にやること」を以下のように更新すること：
- 「大東洋本店へのグラン本店7機能移植：完了（日付）」の記録に置き換え
- 「現在の完成機能」セクションに7機能を追記
- もし今回の作業で新しい教訓（過去に無いパターンの落とし穴）が見つかれば、「移植・大規模改修プレイブック」に追記する

---

## 次のチャットでの開始手順

1. 新しいチャットでClaude Codeを開く（Codeタブ、`pachinko-tool`フォルダを選択）
2. まず`CLAUDE.md`を読ませる（自動で読み込まれるはずだが、念のため「CLAUDE.mdの内容を確認して」と一声かけてもよい）
3. このログファイル（`引き継ぎログ_大東洋本店7機能移植計画.md`）をリポジトリ直下に配置し、コミットする
4. 作業用ブランチを作成する指示を出す
5. Step1（日別島図）から、本ログの「実装手順」9項目に従って着手する
6. 各Stepの完了報告は、このチャットではなく新しいチャットに対して行う（引き継ぎ済みのため、このチャットに戻る必要はない）
